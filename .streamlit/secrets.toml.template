# Plantilla. Copiá este archivo a `secrets.toml` (sin el .template) y reemplazá
# la key. El archivo `secrets.toml` está en .gitignore — no se sube a git.
#
#   cp .streamlit/secrets.toml.template .streamlit/secrets.toml
#   # editar y poner tu key real
#
# Después reiniciá streamlit (st.secrets se carga al iniciar el proceso).

ANTHROPIC_API_KEY = "sk-ant-api03-..."
# EIA: para SPR USA y gas USA semanal. Opcional — sin esta key se usa DEMO_KEY
# (5 req/h, suficiente con el cache a disco). Free: https://api.eia.gov/key/
EIA_API_KEY = ""
